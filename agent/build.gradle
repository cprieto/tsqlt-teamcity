task assemblePlugin(type: Zip, dependsOn: jar) {
	archiveName = "${rootProject.name}-${version}.zip"
	into ('lib') {
		from jar.outputs.files
		from project.configurations.plugin
	}
	from ('.') {
		include 'teamcity-plugin.xml'
	}
	into ('lib/native') {
		from ('native')
	}
}
